{
  "name": "flyrank-capstone-metering-billing",
  "version": "1.0.0",
  "private": true,
  "description": "Usage metering, quota enforcement, cost calculation and Stripe test-mode subscriptions.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "stripe": "^18.5.0"
  }
}
